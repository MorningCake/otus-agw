pluginManagement {
    repositories {
        repositories {
            mavenCentral()
            gradlePluginPortal()
        }
    }
}


enableFeaturePreview("VERSION_CATALOGS")

rootProject.name = 'auth-gateway'
